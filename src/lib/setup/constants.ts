/** Minimum library entries before setup is considered complete. */
export const LIBRARY_SETUP_ENTRY_THRESHOLD = 5;

/** Completed series needed before the ranking CTA appears on setup success. */
export const LIBRARY_SETUP_RANKING_SERIES_THRESHOLD = 2;
